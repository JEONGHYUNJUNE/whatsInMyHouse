# 집에뭐있지 제품 기획

## 제품 정의

우리 집의 모든 식재료가 어디에 있고, 언제까지 먹어야 하며, 무엇을 만들 수 있는지 빠르게 알려주는 주방 인벤토리입니다.

## UX 원칙

- 신규 식재료 등록은 10초 안에 끝냅니다.
- 검색 결과는 두 번의 터치 안에 찾습니다.
- 위치 이동과 소진은 목록에서 바로 처리합니다.
- 포장 소비기한과 농산물 권장 섭취일을 명확히 구분합니다.
- 최초 대량 등록은 공간을 먼저 고르고 여러 품목을 연속 등록하는 방식으로 확장합니다.

## 정보 구조

```text
사용자
└─ 주방
   ├─ 주방 구성원
   ├─ 보관공간
   │  └─ 실제 보유 식재료
   │     └─ 등록·이동·소비·폐기 이력
   ├─ 저장 레시피
   └─ 알림
```

## 보관공간

냉장실, 냉동실, 김치냉장고, 수납장, 팬트리, 싱크대 하부장, 조리대, 사용자 정의 공간을 지원합니다. 공간마다 이름, 별칭, 메모, 색상, 아이콘, 지도 위치와 크기를 가집니다.

## 식재료

상품명, 별칭, 바코드, 사진, 카테고리, 수량과 단위, 구매일, 개봉일, 유통기한, 소비기한, 권장 섭취일, 메모와 현재 위치를 관리합니다.

## 주방맵

기본 주방 블록을 조립하는 방식으로 시작합니다. 향후 드래그, 크기 조절, 실제 주방 사진 배경, 사용자 정의 사각형 영역을 지원합니다.

## 검색

상품명, 별칭, 메모, 카테고리, 바코드와 보관공간을 통합 검색하고 위치·기한·상태별로 필터링합니다.

## 레시피

초기에는 관리자가 주기적으로 레시피와 재료 조합을 DB에 등록합니다. 앱은 현재 재고와 `recipe_ingredients`를 비교해 보유 재료 비율과 임박 재료 사용 여부로 추천합니다. AI 연결 없이도 여러 식재료 조합 추천이 가능합니다.

향후 AI를 연결할 경우 Supabase Edge Function에서 현재 재고와 사용자 조건을 전달하고, 집에 있는 재료와 추가 구매 재료를 구분한 구조화 결과만 받습니다.

## 권장 섭취일

`식재료 + 보관 위치 + 상태(통째/절단/개봉/조리/세척)` 조합의 룰을 관리합니다. 법적인 소비기한이 아닌 권장 섭취 범위와 신선도 확인일을 제안하며 사용자가 수정할 수 있습니다.

## 후속 기능

- 요리 사진, 글, 사용 식재료와 레시피를 연결하는 요리 다이어리
- 다이어리 작성 시 사용 재료 수량 차감
- 고유 친구 코드 기반 친구 추가
- 선택적 공유 다이어리
- 가족 공동 주방 초대

## Google 로그인 재개 지점

현재는 아이디·비밀번호 로그인을 우선 사용합니다. Google OAuth 프론트 코드는 보존하며, Vercel 배포 주소가 확정되면 Supabase Site URL/Redirect URLs와 Google Cloud의 Authorized JavaScript origins에 운영 주소를 추가하는 단계부터 재개합니다. Google의 승인된 리디렉션 URI는 Supabase callback URL을 그대로 유지합니다.

## 아이디 로그인 운영 설정

Supabase Auth에는 일반 아이디 인증이 없어 내부적으로 `아이디@whats-in-my-house.app` 형식의 가상 이메일을 사용합니다. 실제 메일 발송과 반송을 막기 위해 `ignore_username_auth_email` Postgres 함수를 Send Email Auth Hook으로 활성화하고 Email Provider의 `Confirm email`을 끕니다. 초기 버전에서는 이메일 기반 비밀번호 찾기를 제공하지 않습니다.
